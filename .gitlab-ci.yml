stages:
  - init

init_workspace:
  stage: init
  image: alpine:latest
  script:
    - /bin/false
    - touch ${WORKSPACE}/REPORT_SUCCESS
  after_script:
    - if [ -f ${WORKSPACE}/REPORT_SUCCESS ]; then echo success; else echo failure; fi
